{
  "name": "@toshihiko/docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run check && vitepress build . 2>&1",
    "check": "node scripts/check-docs.mjs",
    "dev": "vitepress dev .",
    "preview": "vitepress preview .",
    "test": "npm run check",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitepress": "1.6.4"
  }
}
